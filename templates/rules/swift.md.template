---
globs: **/*.swift
---
# Swift/iOS Rules — {{PROJECT_NAME}}

- SwiftUI preferred; specify minimum iOS target in project config
- Handle loading, error, and empty states explicitly in every view
- Add accessibility identifiers for all interactive elements
- camelCase for variables/functions, PascalCase for types/protocols
- Use `guard` for early returns; avoid force-unwrapping (`!`)
- Prefer value types (`struct`) over reference types (`class`) where appropriate
- Use `async/await` for concurrency (iOS 15+)
- Test with your project's test command (e.g., `xcodebuild test`)

<!-- Add project-specific Swift rules below (e.g., HealthKit, HomeKit, CoreData) -->

<!-- Add project-specific Swift rules below -->
